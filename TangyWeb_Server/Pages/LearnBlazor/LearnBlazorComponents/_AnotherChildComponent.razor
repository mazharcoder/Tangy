<div>
  <h4 class="text-primary pt-3">Another Child Component</h4>
  <!-- If any attribute is placed after the @@attributes that that would preference would be high -->
  @* <input id="nametemp" @attributes="@InputAttributes" placeholder="Child placeholder preference high" /> *@

  <!-- If any attribute is placed before the @@attributes that that would preference would be low -->
  <input id="nametemp" placeholder="Child placeholder preference high" @attributes="@InputAttributes" />
</div>

@code {
  // [Parameter]
  // public string Placeholder { get; set; } = "Initial Text";

  // [Parameter]
  // public string Required { get; set; } = "required";

  // [Parameter]
  // public string MaxLength { get; set; } = "10";

  [Parameter(CaptureUnmatchedValues = true)]
  public Dictionary<string, object> InputAttributes { get; set; } = new Dictionary<string, object>();
}
